import stdlib

# Deklaracija funkcij

func test() {
	print(string: "ta funkcija nima nic argumentov in vraca Void")
}

# Funkcija z enim argumentom tipa Bool, ki vrača Double
func foo(x: Bool) Double {
	if x {
		return 10.0
	}
	else {
		return 50.0
	}
}

test()
print(double: foo(x: true))

# Metode z istim imenom

func metoda() {
	print(string: "metodaA")
}

func metoda(x: Int) {
	print(string: "metodaB")
}

func metoda(double: Double) {
	print(string: "metodaC")
}

func metoda(x: Int, y: Bool) {
	print(string: "metodaD")
}

metoda()
metoda(x: 10)
metoda(double: 1.5)
metoda(x: 0, y: false)

# ime argumenta je lahko sestavljeno iz dveh delov
# iz labele argumenta ter imena parametra
func cellForRow(at indexPath: Char) {
	print(char: indexPath)
}

# izven metode pa ime parametra
cellForRow(at: 'A')

# ime parametra je lahko tudi prazno
func viewController(_ controller: Int) {
	print(integer: controller)
}

viewController(10)
