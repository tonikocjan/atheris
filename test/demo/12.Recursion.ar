import stdlib

func recursivePrint(_ n: Int) {
    if n > 0 {
        recursivePrint(n - 1)
        print(n)
    }
}

recursivePrint(3)

/*print("########")

func fib(_ n: Int) Int {
    if n < 2 {
        return 1
    }
    else {
        return fib(n - 1) + fib(n - 2)
    }
}
print(fib(0))
print(fib(1))
print(fib(2))
print(fib(3))
print(fib(4))
print(fib(5))*/