import stdlib

a is A

int* virtualTablePointer; // naslov virtualne tabele
const int typeDescriptor; // deskriptor razreda
bool result; // končni rezultat

do {
    if (typeDescriptor == *virtualTablePointer) {
        result = true;
        break;
    }
    virtualTablePointer = *(virtualTablePointer + 4);
} while (virtualTablePointer != NULL);