# Razredi

import stdlib

class Test {
    let data = [10, 20, 30]

    func get(indexOf element: Int) Int {
        return -1
    }
}

# objekt se kreira s privzetim konstruktorjem
let object = Test()

let fun = object.get