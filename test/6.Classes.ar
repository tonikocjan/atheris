# Razredi

class Test {
	let a = 10
	let b = 20
	var c = "String"
	
	func memberFn() {
		print(self.a)
		print(self.c)
		self.c = "joze"
		print(c)
	}
}

# objekt se kreira s privzetim konstruktorjem
let object = Test()

object.memberFn()