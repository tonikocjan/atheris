# Razredi

class Test {
	let a = 10
	let b = 20
	var c = "String"
	
	func memberFn() {
		print(self.a)
	}
}

# objekt se kreira s privzetim konstruktorjem
let object = Test()

print(object.a)

func fn(x: Test) {
	print(x.a)
}

fn(x: object)
object.memberFn()