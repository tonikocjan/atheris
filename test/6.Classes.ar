# Razredi

import stdlib

let test = "123"

class Point {
	var x: Int = 5
	var y: Int = 10

	func init(x: Int, y: Int) {
		self.x = x
		self.y = y
	}
}

class Rect {
	var size: Point
	var origin: Point

	func init() {
		self.size = Point(x: 0, y: 0)
		self.origin = Point(x: 0, y: 0)
	}
}

let rect = Rect()
Point(x: 0, y: 0)